import { InterviewLinkProps } from "@/types";

const InterviewLink = ({ interviewId, formData }: InterviewLinkProps) => {
  console.log(interviewId);
  console.log(formData);
  return <div>InterviewLink</div>;
};

export default InterviewLink;
